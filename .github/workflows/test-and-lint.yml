name: Test and lint

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install
        run: make install
      - name: Tests
        run: make test
      - name: Lint
        run: make lint
